===============kodmunki™ temp instructions==============

The /lib directory is where a developer should develop
their JavaScript project. The compilation process will
find all *.js files in this directory and each
subdirectory reading them from top to bottom. The build
process will linking and compile all of the *.js files
into one compressed and one uncompressed JavaScript file
that is output to the /bin file in this project.

This README file can be removed when these instructions
are understood. :{)}